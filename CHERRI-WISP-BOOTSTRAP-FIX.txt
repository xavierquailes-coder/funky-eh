Fix included:
- Adds the missing public/bootstrap-init.js file.
- Loads Scramjet and Epoxy assets from proxy-bootstrap.
- Uses wss://cherrion.top/socket/ as the test Wisp endpoint.
- Keeps Berri hosted locally at http://localhost:3030.

After replacing the files:
1. Stop the old npm server with Ctrl+C.
2. Run npm install.
3. Run npm run dev.
4. Hard refresh with Ctrl+Shift+R.
